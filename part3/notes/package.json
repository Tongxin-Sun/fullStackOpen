{
  "name": "notebackend",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "Tongxin Sun",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "cross-env NODE_ENV=test node --test",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "start": "cross-env NODE_ENV=production node index.js",
    "lint": "eslint .",
    "build:ui": "rm -rf dist && cd ../../introdemo && npm run build && cp -r dist ../part3/demo",
    "deploy:full": "npm run build:ui && cd ../.. && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "supertest": "^7.1.4"
  }
}
